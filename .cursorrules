# 프로젝트 규칙
- 이 프로젝트는 Faster R-CNN을 이용한 자율주행 객체 탐지 프로젝트다.
- 데이터 경로는 `/Users/geonoo/13.상용 자율주행차 주간 도심도로 데이터`를 기본으로 한다.
- 모든 코드는 `dataset.py`, `model.py`, `train.py`로 모듈화하여 관리한다.
- 새로운 기능을 추가할 때는 기존 모듈의 인터페이스를 준수한다.
